{# @author MY-Dev |Mohamed Youssef <mydev@my-dev.pro> #}
{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}
    {{ form_start(zwitchConfigurationForm) }}
    <div class="card">
        <h3 class="card-header">
            <i class="material-icons">settings</i> {{ 'ZWITCH Configurations'|trans({}, 'Modules.Zwitch.Admin') }}
        </h3>
        <div class="card-body">
            <div class="form-wrapper">
                {{ form_widget(zwitchConfigurationForm) }}
            </div>
        </div>
        <div class="card-footer">
            <div class="d-flex justify-content-end">
                <button class="btn btn-primary float-right" id="save-button">
                    {{ 'Save'|trans({}, 'Admin.Actions') }}
                </button>
            </div>
        </div>
    </div>
    {{ form_end(zwitchConfigurationForm) }}
{% endblock %}
